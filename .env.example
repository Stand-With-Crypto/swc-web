# Next.js docs use a .env.local file for local development, but because other libraries
# like prisma need a .env (not .env.local), we standardize everything to .env

# https://github.com/keonik/prisma-erd-generator?tab=readme-ov-file#disabled
DISABLE_ERD=true
# reach out to a collaborator for this info
DO_THEY_SUPPORT_IT_API_KEY=changeMe
NEXT_PUBLIC_THIRDWEB_AUTH_DOMAIN=localhost:3000
NEXT_PUBLIC_ENVIRONMENT=local
# These values can be generated by creating a free account and generating client id/secret at https://thirdweb.com/dashboard/settings
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=changeMe
THIRD_WEB_CLIENT_SECRET=changeMe
# run in node: `new require('ethers').Wallet.createRandom().privateKey`
THIRDWEB_AUTH_PRIVATE_KEY=changeMe
# reach out to a collaborator for this info
DATABASE_URL=changeMe
# ETH address for your local web3 wallet you will be connecting to the app
LOCAL_USER_CRYPTO_ADDRESS=changeMe
