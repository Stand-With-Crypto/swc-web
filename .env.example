# Next.js docs use a .env.local file for local development, but because other libraries
# like prisma need a .env (not .env.local), we standardize everything to .env


# https://github.com/keonik/prisma-erd-generator?tab=readme-ov-file#disabled
DISABLE_ERD=true
# reach out to a collaborator for this info
DO_THEY_SUPPORT_IT_API_KEY=
# If you need to set to this to true, make sure your DO_THEY_SUPPORT_IT_API_KEY is also for production
USE_DTSI_PRODUCTION_API_ON_LOCAL=false
NEXT_PUBLIC_THIRDWEB_AUTH_DOMAIN=localhost:3000
NEXT_PUBLIC_ENVIRONMENT=local
# These values can be generated by creating a free account and generating client id/secret at https://thirdweb.com/dashboard/settings
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=
THIRD_WEB_CLIENT_SECRET=
# run in node: `new require('ethers').Wallet.createRandom().privateKey`
THIRDWEB_AUTH_PRIVATE_KEY=
# If you're a core contributor, follow the steps below:
# - request access to the Stand With Crypto PlanetScale account
# - Once you receive your invite, you'll want to login and go to the `swc-web` database 
# - Click the "Branches" tab
# - Click "New Branch" button
# - Name the branch "yourgithubusername-local" and set the base branch to "testing", press "Create Branch"
# - After the branch has initialized, press the "Connect" button
# - Create a user with the admin role, press "Create Password"
# - In the next screen, find the "Connect With" select dropdown and select "Prisma". Copy paste the resulting .env variable into your .env file
# Alternatively, you can spin up your own planetscale account
DATABASE_URL=
# ETH address for your local web3 wallet you will be connecting to the app
LOCAL_USER_CRYPTO_ADDRESS=
NEXT_PUBLIC_LOG_LEVEL=info
SUPPRESS_SENTRY_ERRORS_ON_LOCAL=true
# Only needed when you want to log out the sql queries coming from prisma
# LOG_DATABASE=true
# Only needed when you want to quickly test npm run build locally. Rarely needed.
# SPEED_UP_LOCAL_BUILDS=true
# reach out to a collaborator for this info
# NEXT_PUBLIC_SENTRY_DSN=changeMe
# reach out to a collaborator for this info
NEXT_PUBLIC_GOOGLE_CIVIC_API_KEY=
# reach out to a collaborator for this info
NEXT_PUBLIC_GOOGLE_PLACES_API_KEY=
# If you want to test the cookie consent banner locally without having to clear the cookie every time, set this to true
NEXT_PUBLIC_DEBUG_COOKIE_CONSENT=false
# reach out to a collaborator for this info
NEXT_PUBLIC_MIXPANEL_PROJECT_TOKEN=