'use client'

import * as React from 'react'
import {
  VisuallyHidden as RadixVisuallyHidden,
  VisuallyHiddenProps,
} from '@radix-ui/react-visually-hidden'

export const VisuallyHidden = (props: VisuallyHiddenProps) => <RadixVisuallyHidden {...props} />
