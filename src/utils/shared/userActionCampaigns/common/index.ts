// Since we want to show all opt-ins as done in different countries when the user logs in,
// we're unifying the campaign name for all opt-ins
export enum UserActionOptInCampaignName {
  DEFAULT = 'DEFAULT',
}
