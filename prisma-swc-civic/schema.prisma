generator client {
  provider        = "prisma-client-js"
  output          = "../src/data/prisma/generated/swc-civic"
  previewFeatures = ["driverAdapters", "postgresqlExtensions"]
}

datasource db {
  provider   = "postgresql"
  url        = env("SWC_CIVIC_DATABASE_URL")
  extensions = [postgis(schema: "public")]
}

model spatial_ref_sys {
  srid      Int     @id
  auth_name String? @db.VarChar(256)
  auth_srid Int?
  srtext    String? @db.VarChar(2048)
  proj4text String? @db.VarChar(2048)
}

model constituencies {
  id          Int                                      @id @default(autoincrement())
  name        String
  stateCode   String?                                  @map("state_code")
  wkbGeometry Unsupported("geometry(GeometryZ,4326)")? @map("wkb_geometry")
  countryCode String?                                  @map("country_code")

  @@index([wkbGeometry], map: "constituencies_wkb_geometry_geom_idx", type: Gist)
}
